name: deploy

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: self-hosted
    if: false # Disable the job, remove this line to enable it
    steps:
      - uses: actions/checkout@v4
      - name: Run deploy script
        run: |
          bash deploy.sh stickertiler